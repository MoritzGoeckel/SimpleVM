#pragma once

using idx_t = size_t;
#define idx_s sizeof(idx_t)

using int_t = int;
#define int_s sizeof(int_t)

using byte_t = char;
#define byte_s sizeof(byte_t)

using bool_t = char;
#define bool_s sizeof(bool_t)

